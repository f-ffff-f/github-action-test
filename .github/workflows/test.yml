name: '[test]'

on:
  push:
    branches:
      - 'main'

jobs:
  deploy-develop:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          clean: false

      - name: where am i?
        run: pwd

      - name: hi?
        run: echo "i don't know"

      - name: Check GITHUB_WORKSPACE
        run: echo "GITHUB_WORKSPACE=$GITHUB_WORKSPACE"
